[pytest]
addopts = -ra
markers =
    integration: marks integration tests
# Restrict discovery to the server project tests
testpaths = innovation-engine-mcp/tests
# Ensure src is on PYTHONPATH when running from repo root
pythonpath = innovation-engine-mcp/src
